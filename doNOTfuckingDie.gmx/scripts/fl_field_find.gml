///fl_field_find(name)
/*
    Returns the component instance with the given name.
*/

var component = noone;

with(obj_fl_component) {
    if(name == argument0) {
        component = id;
        break;
    }
}

return component;
