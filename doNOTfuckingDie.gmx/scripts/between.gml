/// between(n, min, max)
/// returns true if n is in the range [min, max] (inclusive)

{
    var _n, _min, _max;
    _n = argument0;
    _min = argument1;
    _max = argument2;
    
    return ( (_min<=_n)&&(_n<=_max) )
}
