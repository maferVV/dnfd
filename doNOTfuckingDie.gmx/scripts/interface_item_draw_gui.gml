/// interface_item_draw_gui()
draw_sprite_ext(sprite_index,
                dt_image_index,
                x,
                y + yoffset,
                image_xscale + xscale_offset,
                image_yscale + yscale_offset,
                image_angle + angle_offset,
                image_blend,
                image_alpha);
