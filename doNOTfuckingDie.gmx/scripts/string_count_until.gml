/// string_count_until(substr, string, n)
/***************************************************
  Counts appereances but only until the nth spot
 ***************************************************/

return string_count(argument0, string_limit(argument1, argument2, ""));
